package solution._3_replace_method_with_method_object;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

class ReplaceMethodWithMethodObject1Test {

	@DisplayName("gamma 메소드에 인자값을 100(입력값), 3(수량), 500(년수) 입력하면 349860을 반환한다")
	@Test
	void gamma1() {
	}

	@DisplayName("gamma 메소드에 인자값을 100(입력값), 4(수량), 500(년수) 입력하면 349800을 반환한다")
	@Test
	void gamma2() {
	}
}